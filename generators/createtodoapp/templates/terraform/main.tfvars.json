{
    "location": "${AZURE_LOCATION}",
    "environment_name": "${AZURE_ENV_NAME}",
    "principal_id": "${AZURE_PRINCIPAL_ID}",
    "web_image_name": "${SERVICE_WEB_IMAGE_NAME}",
    "api_image_name": "${SERVICE_API_IMAGE_NAME}",
    "client_id": "${ARM_CLIENT_ID}"
}